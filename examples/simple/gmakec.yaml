description: Simple gmakec sample
version: "1.0.0"

targets:
  - compiler:
      path: gcc
      flags:
        - -Wall
        - -Wextra
        - -pedantic
    sources:
      - path: src/*.c
    includes:
      - include/**
    output: build/simple-example
