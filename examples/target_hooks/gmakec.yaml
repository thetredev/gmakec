description: Target hooks gmakec sample
version: "1.0.0"

compilers:
  - name: gcc-default
    path: gcc
    flags:
      - -Wall
      - -Wextra
      - -pedantic

targets:
  - compiler:
      ref: gcc-default
    sources:
      - path: main.c
    output: build/hooks
    hooks:
      - step: preConfigure
        shell: /bin/bash -c
        command: echo "preConfigure independent first"
      - step: preConfigure
        shell: /bin/bash -c
        command: echo "preConfigure independent second"
      - step: postConfigure
        shell: /bin/bash -c
        command: echo "postConfigure independent first"
      - step: postConfigure
        shell: /bin/bash -c
        command: echo "postConfigure independent second"
      - step: preBuild
        shell: /bin/bash -c
        command: echo "preBuild independent first"
      - step: preBuild
        shell: /bin/bash -c
        command: echo "preBuild independent second"
      - step: postBuild
        shell: /bin/bash -c
        command: echo "postBuild independent first"
      - step: postBuild
        shell: /bin/bash -c
        command: echo "postBuild independent second ${HOME}"
